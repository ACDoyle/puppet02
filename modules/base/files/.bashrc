# .bashrc

# User specific aliases and functions

alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'
alias c=clear
alias ee=exit
alias ll='ls -ltr'
alias tt=tree

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi
